# --- Python / backend ---
backend/venv/
**/__pycache__/
*.pyc
backend/instance/
*.db
*.sqlite3

# --- Environment secrets (never commit real secrets) ---
.env
backend/.env

# --- Node / frontend build tooling ---
frontend/node_modules/

# --- OS / editor cruft ---
.DS_Store
.vscode/
.idea/

# NOTE: frontend/static/css/output.css is intentionally NOT ignored.
# Render only runs `pip install`, not `npm run build:css`, so the
# compiled Tailwind CSS must be committed to the repo.